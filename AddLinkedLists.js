var addTwoNumbers = function(l1, l2) {
    // reverse both arrays
    
    // put them in linked lists

    
    
};

function ListNode(val, next) {
    this.val = (val===undefined ? 0 : val)
    this.next = (next===undefined ? null : next)
}



let firstList = [2,4,3];
let secondList = [5,6,4];
addTwoNumbers(firstList, secondList);